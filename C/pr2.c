#include <stdio.h>

int main (void) {

    int x = 2;
    int y = 4;

    if (x < y){
        printf("x is less than y");
    }else if (x > y){
        printf("x is more than y");
    }else {
        printf("x is equal than y");
    }
}